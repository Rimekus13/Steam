[pytest]
testpaths = tests
norecursedirs = logs .logs airflow_logs build dist .git .venv venv env node_modules data .state dags output coverage htmlcov
addopts = -q
filterwarnings =
    ignore:Parsing dates involving a day of month without a year specified is ambiguous:DeprecationWarning